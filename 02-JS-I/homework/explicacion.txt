
 * Variables: es como una caja bacia, en la que podemos agregar un elemento 
 * Strings: es una  cadena de letras que forman palabras
 * Funciones (argumentos, `return`): es lo que utilizamos para poder retornar algo 
 * Declaraciones `if`: el para comprobar si una condicion se cumplio 
 * Valores booleanos (`true`, `false`): solo son dos , o es verdadero o es falso